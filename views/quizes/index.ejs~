<!DOCTYPE html>
<html>
  <head>
	<title>Página Principal</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
<body>
  <% var i; for (i=0; i < quizes.length; i++) { %>  <% } %>      
  <table><tr><td><a href="quizes/<%= quizes[i].id %>"><%= quizes[i].pregunta %></a></td></tr></table>

</body>
</html>
